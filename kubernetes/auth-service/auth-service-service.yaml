apiVersion: v1
kind: Service

metadata:
  name: auth-service
  namespace: devops-project

spec:

  selector:
    app: auth-service

  ports:

  - name: http

    protocol: TCP

    port: 8081

    targetPort: 8081

  type: ClusterIP
