apiVersion: v1
kind: Service

metadata:
  name: backend-service
  namespace: devops-project

spec:

  selector:
    app: backend-service

  ports:

  - name: http

    protocol: TCP

    port: 8082

    targetPort: 8082

  type: ClusterIP
