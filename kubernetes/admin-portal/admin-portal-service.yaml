apiVersion: v1
kind: Service

metadata:
  name: admin-portal-service
  namespace: devops-project

spec:

  selector:
    app: admin-portal

  ports:

  - protocol: TCP
    port: 8083
    targetPort: 8083

  type: ClusterIP
